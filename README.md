# opticlock clock laser controller for ARTIQ

```
conda create -n menlo python=3.5 pyqt=5.6
conda install -c conda-forge websockets
pip install git+https://github.com/MenloSystems/pywebchannel.git
```
